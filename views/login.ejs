<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('header.ejs') %>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        /* background-color: #050D22; */

        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        height: 100vh;
        margin: 0;
      }

      .video-background {
        background: linear-gradient(to right, #62b0f9, #2cbbc8);
        background-size: cover;
        background-position: center;
        width: 100vw;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        /* background-image: url('/picture/Picture3.png'); */
        /* background-color: #050D22; */
        position: relative;
        overflow: hidden;
      }

      /* .video {
    position: relative;
    overflow: hidden;
    z-index: 0;
    justify-content: center;
  }
   
  .video-background video {
    right: 0;
    transition: transform 1s ease;
  } */

      .container {
        background-color: #fff;
        border-radius: 30px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.35);
        position: absolute;
        overflow: hidden;
        width: 768px;
        max-width: 100%;
        min-height: 480px;
      }

      /* .container p {
    font-size: 14px;
    line-height: 20px;
    letter-spacing: 0.3px;
    margin: 20px 0;
  } */

      #NameUser,
      #DeptUser {
        font-weight: bold;
        font-size: 16px;
        color: #03143f;
        border: 1px solid #5c6bc0;
        border-radius: 20px;
        background: transparent;
        text-align: center;
      }

      .input-box i {
        position: absolute;
        right: 85px;
        transform: translateY(-50%);
        font-size: 20px;
        color: #5c6bc0;
      }

      #iconNameUser {
        top: 64%;
      }

      #iconDeptUser {
        top: 78%;
      }

      .container button {
        background-color: #3a5ff6;
        border: 1px solid transparent;
        /* กำหนดความหนาของขอบและสีของขอบ */
        border-radius: 8px;
        color: #fff;
        /* สีของตัวหนังสือในปุ่ม */
        padding: 10px 45px;
        /* กำหนดระยะห่างภายในปุ่ม */
        font-size: 16px;
        /* ขนาดของตัวหนังสือ */
        font-weight: 600;
        cursor: pointer;
        /* ให้เม้าส์เปลี่ยนเป็นรูปมือเมื่อชี้อยู่บนปุ่ม */
        transition: background-color 0.3s, color 0.3s;
        /* ให้ปุ่มมีการเปลี่ยนแปลงอย่างราบรื่นเมื่อมีการโต้ตอบ */
        text-transform: uppercase;
        margin-top: 10px;
      }

      /* .container button.hidden {
    background-color: transparent;
    border-color: #fff;
   
  } */

      .container .form {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        padding: 0 40px;
        height: 100%;
      }

      .container input {
        background-color: #eee;
        border: none;
        margin: 8px 0;
        padding: 10px 15px;
        font-size: 13px;
        border-radius: 8px;
        width: 100%;
        outline: none;
      }

      .form-container {
        position: absolute;
        top: 0;
        height: 95%;
        transition: all 0.6s ease-in-out;
      }

      .icon-person {
        margin-bottom: 20px;
        color: #03143f;
        font-size: 8rem;
        height: 170px;
        text-align: center;
      }

      .sign-in {
        left: 0;
        width: 50%;
        z-index: 2;
      }

      /* .container.active .sign-in {
    transform: translateX(100%);
  } */

      .sign-up {
        left: 0;
        width: 50%;
        opacity: 0;
        z-index: 1;
      }

      .container.active .sign-up {
        transform: translateX(100%);
        opacity: 1;
        z-index: 5;
        animation: move 0.6s;
      }

      @keyframes move {
        0%,
        49.99% {
          opacity: 0;
          z-index: 1;
        }

        50%,
        100% {
          opacity: 1;
          z-index: 5;
        }
      }

      .toggle-container {
        position: absolute;
        top: 0;
        left: 50%;
        width: 50%;
        height: 100%;
        overflow: hidden;
        transition: all 0.6s ease-in-out;
        z-index: 1000;
        border-radius: 150px 0 0 100px;
      }

      .container.active .toggle-container {
        transform: translateX(-100%);
        border-radius: 0 150px 100px 0;
      }

      .toggle {
        background-color: #4bcbc1;
        height: 100%;
        background: linear-gradient(to right, #62b0f9, #2cbbc8);
        color: #fff;
        position: relative;
        left: -100%;
        height: 100%;
        width: 200%;
        transform: translateX(0);
        transition: all 0.6s ease-in-out;
      }

      .container.active .toggle {
        transform: translateX(50%);
      }

      .toggle-panel {
        position: absolute;
        width: 50%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        padding: 0 30px;
        text-align: center;
        top: 0;
        transform: translateX(0);
        transition: all 0.6s ease-in-out;
      }

      .toggle-left {
        transform: translateX(-200%);
      }

      .container.active .toggle-left {
        transform: translateX(0);
      }

      .toggle-right {
        right: 0;
        transform: translateX(0);
      }

      .container.active .toggle-right {
        transform: translateX(200%);
      }
    </style>
  </head>

  <body>
    <main data-router-wrapper>
      <section class="login" data-router-view="login">
        <div class="video-background">
          <!-- <video autoplay loop muted playsinline id="myVideo">
                    <source src="/picture/globe-900.hevc-58019d77b21c.mp4" type="video/mp4">
                    Your browser does not support the video tag.
                </video> -->
          <div class="container" id="container">
            <div class="form-container sign-in">
              <div class="form">
                <img
                  src="/photo/LOGO_DevicesControl3.png"
                  class="icon-person"
                  alt="Person"
                />

                <div class="input-box">
                  <input
                    type="text"
                    id="NameUser"
                    value="<%= Name %>"
                    readonly
                  />
                  <i class="bi bi-person-fill" id="iconNameUser"></i>
                </div>
                <div class="input-box">
                  <input
                    type="text"
                    id="DeptUser"
                    value="<%= Dept %>"
                    readonly
                  />
                  <i class="bi bi-briefcase-fill" id="iconDeptUser"></i>
                </div>
              </div>
            </div>
            <div class="toggle-container">
              <div class="toggle">
                <div class="toggle-panel toggle-left">
                  <h1></h1>
                  <p style="line-height: 1">
                    High performance and Monitoring Overall of Production
                    process
                  </p>
                  <p style="line-height: 0">
                    Now Phase I : Assembly & Engineering Board
                  </p>
                  <button type="button" class="hidden" id="SignUp">
                    <i class="bi bi-arrow-left"></i> Welcome to NIA
                  </button>
                </div>
                <div class="toggle-panel toggle-right">
                  <h4>Smart Devices control system</h4>
                  <p style="line-height: 1">
                    Borrow, Return, Transfer and monitoring devices by Web
                    Application.
                  </p>
                  <button
                    type="button"
                    class="hidden"
                    data-nid="<%= NID %>"
                    id="SignIn"
                  >
                    Login<i class="bi bi-arrow-right"></i>
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
  </body>
  <script>
    let SignIn = document.getElementById("SignIn");
    SignIn.addEventListener("click", function () {
      window.location.href = "/devices/request";
    });
  </script>
</html>
